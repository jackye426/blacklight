# Unknowns — ultraworkers/claw-code

> Generated by Blacklight 0.1.0 on 2026-07-16T14:35:31.865Z.
>
> These are things Blacklight could **not** determine. They are recorded, never guessed. See
> RESEARCH-POLICY.md.

## Coverage gap: Rust/Python harness implementation

The V1 static-analysis pipeline maps TypeScript/JavaScript semantics. `ultraworkers/claw-code`
is primarily useful to Blacklight as a public Rust harness artifact, so this pass should be
read as a directory/component inventory rather than a semantic architecture map.

Observed extension counts in the cached source tree at ingest time:

| Extension | Count | Coverage in V1 |
| --- | ---: | --- |
| `.rs` | 101 | File inventory/component skeleton only; Rust imports/types/calls are not semantically mapped |
| `.py` | 82 | File inventory/component skeleton only; Python imports/types/calls are not semantically mapped |
| `.md` | 49 | Documentation inventory only |
| `.json` | 96 | Configuration/data inventory only |

The generated architecture skeleton records 40 inferred components but 0 observed file nodes,
0 concepts, and 0 edges. An automated deep map needs a Rust/tree-sitter adapter, and possibly a
Python adapter for the companion/reference workspace.

## Remaining after manual source study

The direct study in `source-study.md` resolves the high-level runtime shape, but these claims remain
unknown or unverified:

- **Historical lineage:** the cached repository is a depth-one clone with one visible commit. It
  cannot prove how the code evolved or whether any particular implementation detail descends from
  earlier third-party material.
- **Canonical Anthropic provenance:** the repository's own disclaimer rejects this claim; no local
  evidence independently establishes it.
- **Current test status:** 1,416 Rust test attributes and CI commands were inventoried, but Cargo is
  unavailable in the Blacklight environment, so the suite was not executed.
- **Live provider behavior:** no credentials or external provider endpoints were used in this study.
- **Runtime depth outside the primary path:** the Python/reference tree, the autonomous maintenance
  metadata under `.omx`, and the separate RAG service were inventoried but not traced to the same
  depth as the `claw` binary.

The automated graph remains unchanged. Manual findings should not be mistaken for Rust semantic
coverage in Atlas itself.
