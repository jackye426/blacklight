# Unknowns — openai/codex

> Generated by Blacklight 0.1.0 on 2026-07-16T14:12:53.569Z.
>
> These are things Blacklight could **not** determine. They are recorded, never guessed. See
> RESEARCH-POLICY.md.

## Coverage gap: Rust core

The V1 static-analysis pipeline maps TypeScript/JavaScript semantics. The Codex repository
contains a large Rust implementation surface, so this pass should not be read as a complete
Codex architecture map.

Observed extension counts in the cached source tree at ingest time:

| Extension | Count | Coverage in V1 |
| --- | ---: | --- |
| `.rs` | 2,604 | File inventory only; Rust imports/types/calls are not semantically mapped |
| `.ts` | 632 | TypeScript file/concept/import analysis applies |

The generated graph therefore captures TypeScript-facing surfaces such as `codex-cli`,
`sdk/typescript`, and generated TypeScript schemas under `codex-rs`, but the Rust core needs
a future tree-sitter or Rust-specific adapter before Blacklight can make structural claims
about it.
